import 'package:flutter/material.dart';

class Test1Page extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("test1"),
      ),
      body: Center(
        child: Text("Test1 page"),
      ),
    );
  }
}
